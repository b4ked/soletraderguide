/**
 * SoleTraderGuide root layout.
 * Imports base globals first, then site brand overrides, then delegates to DNA layout.
 */
import '../../base/src/app/globals.css'
import './globals.css'

export { default, metadata } from '../../base/src/app/layout'
